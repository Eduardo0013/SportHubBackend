import { v4 } from 'uuid'
import prisma from "../../../config/prisma"
import User from "../../core/domain/models/Usuario"

export const authenticate = (user : unknown) => {
    /*const userModel : User = {
        id : v4(),
        nombre : user.name,
    }*/
}